# Environment files (don't include dev configs in production images)
backend/.env
frontend/.env
.env
.env.local
.env.development

# NOTE: .env.prod files should NOT be in .dockerignore as they need to be available at runtime
# They are loaded via env_file in docker-compose, not copied during build


# Git and development files
.git
.gitignore
README.md
*.md

# Node modules (will be installed in container)
node_modules
*/node_modules

# Development and build artifacts
.DS_Store
.vscode
.cursor
*.log